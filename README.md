
  # Create Custom Bank Interface

  This is a code bundle for Create Custom Bank Interface. The original project is available at https://www.figma.com/design/omskiXdsVtrWxn6vofNqRM/Create-Custom-Bank-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  